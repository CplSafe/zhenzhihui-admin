# 开发环境配置
# 远程后端 / DeepAuth 源(vite 代理把 /api、/auth、/deepauth 打到这里)。
# 照搬 zhenzhihui-web 的本地连服务器方案。
VITE_ZZH_REMOTE_ORIGIN=http://115.190.63.215:9000
VITE_DEEPAUTH_REMOTE_ORIGIN=http://115.190.63.215:8080

# 前端请求的 API 基础路径(走 vite 代理时保持 /api/v1)
VITE_API_BASE_URL=/api/v1

# DeepAuth JSON API 的本地代理前缀(登录页调用)
VITE_DEEPAUTH_API_BASE_URL=/deepauth
