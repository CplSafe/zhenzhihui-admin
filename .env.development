# 开发环境配置
# 后端业务服务地址(Vite dev server 把 /api 与 /auth 代理到这里)
VITE_API_PROXY_TARGET=http://115.190.63.215:8001

# 前端请求的 API 基础路径(走 vite 代理时保持 /api/v1)
VITE_API_BASE_URL=/api/v1

# DeepAuth OAuth 登录入口(后端代理路径)。未登录时前端跳到这里。
VITE_LOGIN_URL=/auth/login
