# 生产环境配置(部署到 nginx 同源,/api、/auth、/deepauth 反代到后端 / DeepAuth)
VITE_API_BASE_URL=/api/v1
VITE_DEEPAUTH_API_BASE_URL=/deepauth

# 远程源:用于把后端/DeepAuth 返回的绝对地址(login_api_url、redirect_to)
# 改写成本地 /deepauth、/auth 同源路径,交给 nginx 反代。部署时按实际后端地址调整。
VITE_ZZH_REMOTE_ORIGIN=http://115.190.63.215:9000
VITE_DEEPAUTH_REMOTE_ORIGIN=http://115.190.63.215:8080
